{
  "name": "xburger-pdv",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "sh -c 'npm run dev:site & npm run dev:sistema & wait'",
    "dev:site": "npm run dev --prefix SITElanchesdoben",
    "dev:sistema": "vite",
    "build": "npm run build:site && npm run build:sistema && npm run build:merge",
    "build:site": "npm ci --include=dev --prefix SITElanchesdoben && npm run build --prefix SITElanchesdoben -- --outDir ../.tmp/site-dist",
    "build:sistema": "vite build --outDir .tmp/sistema-dist --base=/sistema/",
    "build:merge": "node scripts/merge-builds.mjs",
    "preview": "vite preview"
  },
  "dependencies": {
    "framer-motion": "^12.35.2",
    "lucide-react": "^0.577.0",
    "react": "^19.2.4",
    "react-dom": "^19.2.4",
    "recharts": "^3.7.0",
    "tailwind-merge": "^3.5.0"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "@vitejs/plugin-react": "^5.0.0",
    "typescript": "~5.8.2",
    "vite": "^6.2.0"
  }
}
